# AGENT.md

Use `AGENTS.md` as the authoritative file.

Read `docs/thread-merge-methodology.md` when working on Reddit ingest, thread reconstruction, orphan handling, or corpus-prep changes.
Read `docs/avatar-state-research-system.md` when working on avatar slicing, beachhead state clusters, workspace refreshes, or adjacent sub-avatar expansion.

This alias exists for humans and for fallback discovery configured in `.codex/config.toml`.
